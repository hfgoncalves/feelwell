fieldCatalog = [];

fieldCatalog.push({ name: "createdBy", label: "Who", type: "text"});
fieldCatalog.push({ name: "points", label: "Points", type: "text" });

result.data = fieldCatalog;

complete();